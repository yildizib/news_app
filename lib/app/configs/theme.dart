import 'package:flutter/material.dart';

class AppTheme {
  static final lightThemeDate = ThemeData(brightness: Brightness.light);

  static final darkThemeDate = ThemeData(brightness: Brightness.dark);
}
